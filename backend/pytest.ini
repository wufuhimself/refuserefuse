[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    security: asserts an authorization or data-scoping boundary
    privacy: asserts a location-privacy guarantee (consent, minimization, deletion)
